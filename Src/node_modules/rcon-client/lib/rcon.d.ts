import { Disposable } from "event-kit";
export declare class Rcon {
    private options;
    private emitter;
    private sendPacketQueue;
    private socket;
    private requestId;
    connecting: boolean;
    authenticated: boolean;
    constructor(options?: {
        packetResponseTimeout?: number;
    });
    onDidConnect(callback: () => any): Disposable;
    onDidAuthenticate(callback: () => any): Disposable;
    onDidDisconnect(callback: () => any): Disposable;
    connect(options: {
        host: string;
        port: number;
        password: string;
    }): Promise<void>;
    disconnect(): Promise<void>;
    end(): Promise<void>;
    send(command: string): Promise<string>;
    private sendPacket(type, payload, isAuth?);
    private onPacket(callback);
    private subscribeToSocketEvents();
}
