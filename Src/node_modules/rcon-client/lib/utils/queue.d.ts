export declare class PromiseQueue {
    private queue;
    private pendingPromisesCount;
    private maxConcurrent;
    private paused;
    constructor(options?: {
        maxConcurrent?: number;
    });
    add<T>(promiseGenerator: () => Promise<T>): Promise<T>;
    pause(): void;
    resume(): void;
    private dequeue();
}
